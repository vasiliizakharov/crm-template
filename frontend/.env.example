# Frontend build-time env (consumed by Vite).
# Copy to .env before `npm run build` if you want a non-default API URL.

# Absolute or relative URL where the backend is reachable.
# Empty / unset means same-origin /api (recommended when nginx proxies /api/).
VITE_API_URL=
